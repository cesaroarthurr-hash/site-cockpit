/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // Permettre les images locales dans /public
    // Ajouter ici les domaines externes si besoin
    remotePatterns: [],
  },
};

export default nextConfig;
